/**
 * App-wide constants. Import from `@/src/constants`.
 *
 * Distinct from `@/src/theme`, which holds the design *scales* — see the note
 * at the top of `layout.ts` for where the line falls.
 */
export { NAV_CLEARANCE, MIN_TOUCH_TARGET } from "./layout";
